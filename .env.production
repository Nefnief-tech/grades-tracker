# Production Environment Configuration
NODE_ENV=production
NEXT_PUBLIC_DEPLOYMENT_ENV=production

# Asset Path Configuration - ensure static assets load correctly
NEXT_PUBLIC_ASSET_PREFIX=

# When deployed to a custom domain, you might need to set a base path
# NEXT_PUBLIC_BASE_PATH=

# PWA Configuration
NEXT_PWA_DISABLE=false
NEXT_PWA_REGISTER=true

# Build Configuration
NEXT_STRICT_MODE=true